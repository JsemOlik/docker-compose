http:
  routers:
    immich:
      rule: Host(`immich.example.com`)
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt
      service: immich

  services:
    immich:
      loadBalancer:
        servers:
          - url: "http://10.1.0.69:30041"
