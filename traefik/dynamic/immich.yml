http:
  routers:
    immich:
      rule: Host(`immich.jsemolik.dev`)
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt
      service: immich

  services:
    immich:
      loadBalancer:
        servers:
          - url: "http://10.1.0.30:30041"
